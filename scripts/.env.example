# Maintainers only. Candidates do not need this file.
# Comma-separated Sanity project IDs, one per candidate
SANITY_PROJECT_IDS=
# Comma-separated write tokens (Editor role), in the same order as SANITY_PROJECT_IDS
SANITY_TOKENS=
# Dataset to seed in every project
SANITY_DATASET=production
