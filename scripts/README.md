# Seed script (maintainers only)

Writes the homepage content in `seed-content.ts` to every candidate's Sanity project, using the fixed ID `homePage`. You can safely run it more than once.

Copy `scripts/.env.example` to `scripts/.env`, fill it in (tokens need Editor rights on each project), then run `npm run seed` from the repo root.
